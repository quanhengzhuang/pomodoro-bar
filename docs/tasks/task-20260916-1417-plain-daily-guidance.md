# 简化今日指引展示

## 目标

将今日指引改为纯文本展示，并确保较多行内容可以完整显示。

## 改动

- 移除无序列表、有序列表和加粗解析，只保留手动换行与长行自动折行。
- Markdown 标记按普通文字显示，不改变现有 `daily-guidance.json` 数据格式或内容。
- 整段内容统一使用暖黄色斜体展示，不提供按内容设置的富文本格式。
- 将菜单内容宽度从 320 点提高到 520 点，让每行展示更多文字并减少不必要的折行。
- 使用文本布局的实际高度设置菜单内容区域，避免多行内容被截断。
- 移除编辑弹窗中的富文本格式提示。
- 同步更新中英文 README。

## 影响文件

- `Sources/main.swift`
- `README.md`
- `docs/en/README.md`
- `docs/tasks/task-20260916-1417-plain-daily-guidance.md`
- `docs/en/tasks/task-20260916-1417-plain-daily-guidance.md`

## 预估代码行数

删除约 80–100 行，新增或调整约 30–50 行代码及少量文档。

## 实际变更

- 删除列表、编号和加粗解析，原有格式标记现在按普通文字显示。
- 今日指引改为 520 点宽的暖黄色统一斜体纯文本，仅保留手动换行和自动折行。
- 使用文本布局管理器的实际排版高度设置菜单视图，避免多行内容被截断。
- 编辑弹窗和中英文 README 已移除富文本格式说明。
- `daily-guidance.json` 的路径、结构和已有内容保持不变。

## 验证结果

- `./scripts/build.sh` 构建成功，仅有既有通知 API deprecated 警告。
- `git diff --check` 通过，源码中已无列表或加粗解析方法。
