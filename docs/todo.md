TODO List:
- 时间超过 5min 就要记录，现状是什么样的？task 要先写现状。
- 番茄外时间，到 now，现状是什么样的？
- 运行中状态旁边，加上备注，以及加备注按钮也放在那。现在我不知道我的备注加的是什么，有没有加上。
- 全部记录改为每一天的记录，还是子菜单，显示近 7 天。近 10 次的记录还保留。
- 清空记录按钮太危险，去掉吧。可以增加打开记录 json 文件的入口，要清空手动删除即可。
- 运行中状态，时间可以延长，弹出一个框，默认填写 5 分钟，类似设置专注时长。设置专注时长入口可以去掉了，专注默认还是 25 分钟。
- 运行最新的程序，原来已经运行中的程序能不能自动退出？
- 应用的 icon 和番茄的状态栏 icon 一致，检查所有地方的 icon
- [x] 弹出菜单时，只按空格暂停；再按空格继续。
- 「重置」「跳到下一段」有意义吗？
- 所有文档，增加中文版、英文版两版，readme 中增加语言切换链接
